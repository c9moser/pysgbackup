[archiver]
cygpath = __CYGPATH__
executable = __EXECUTABLE__
verbose = -v
extension = tbz
create = -cjf "${FILENAME}" ${VERBOSE} -C "${ROOT_DIR}" "${BACKUP_DIR}"
extract = -xjf "{FILENAME} ${VERBOSE} -C "${ROOT_DIR}"
